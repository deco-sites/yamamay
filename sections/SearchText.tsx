import SearchText, {
  Props,
} from "deco-sites/fashion/components/search/SearchText.tsx";

function SearchTextSection(props: Props) {
  return <SearchText {...props} />;
}

export default SearchTextSection;
